//훅 테스트 작성
import { describe, expect, test } from 'vitest';

describe('단위테스트 : 반복일정 관리', () => {
  describe('useSchedule 훅', () => {
    test('일정생성, 수정시 반복 일정을 체크하여 UI가 표시된다 ', () => {
      expect('').toBe('');
    });
  });
});
