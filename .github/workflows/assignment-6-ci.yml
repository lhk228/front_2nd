name: CI Assignment 6

on:
  pull_request:
    types: [opened, synchronize, reopened]
    # paths:
    #   - 'packages/assignment-6/**'
  push:
    branches: [develop]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: List root files
        run: ls -R
        # 루트 디렉토리 구조 출력

      - name: List packages files
        run: ls -R packages
        # `packages` 디렉토리 및 하위 디렉토리 구조 출력

      - name: List assignment-6 files
        run: ls -R packages/assignment-6
        # `packages/assignment-6` 디렉토리의 파일 리스트 출력

      - name: job1 구현
        run: npm run build
        working-directory: packages/assignment-6

  lint:
    runs-on: ubuntu-latest
    steps:
      - name: job2 구현
        run: echo "job2의 내용을 구현해주세요"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: job3 구현
        run: echo "job3의 내용을 구현해주세요"

  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - name: job4 구현
        run: echo "job4의 내용을 구현해주세요"

  assign_reviewer:
    runs-on: ubuntu-latest
    steps:
      - name: job5 구현
        run: echo "job5의 내용을 구현해주세요"
